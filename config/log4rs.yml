refresh_rate: 5 seconds
appenders:
  stdout:
    kind: console
    encoder:
      pattern: "{d(%Y-%m-%d %H:%M:%S)} | {({l}):5.5} | {f}:{L} — {m}{n}"
  file:
    kind: file
    path: "log/server.log"
    encoder:
      pattern: "{d(%Y-%m-%d %H:%M:%S)} | {({l}):5.5} | {f}:{L} — {m}{n}"
root:
  level: debug
  appenders:
    - stdout
    - file
loggers:
  qtable:
    level: debug
    appenders:
      - file
      - stdout
    additive: false
